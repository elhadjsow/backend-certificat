# 🧾 Backend - Gestion des Certificats de Domicile

Ce projet représente le **microservice Backend** de gestion des certificats de domicile (API REST Django + PostgreSQL).

**✅ Pipeline CI/CD automatisé avec Jenkins et Docker Hub !**

## 🚀 Démarrage local avec Docker Compose

```bash
docker-compose up --build
